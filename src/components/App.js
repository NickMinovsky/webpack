import React, { useState } from 'react';
import { hot } from 'react-hot-loader';

const App = () => {
  return (
    <div>
      <h1>Welcome to Roomate Boilerplate</h1>
    </div>
  );
};

export default hot(module)(App);
